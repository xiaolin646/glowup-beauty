name = "glowup-beauty"
compatibility_date = "2026-04-18"

# Pages构建配置
[build]
command = "cd poc/frontend-react && npm install && npm run build"
output_directory = "poc/frontend-react/dist"

# 环境变量（如果需要）
# [vars]
# API_URL = "https://your-api.com"

# 自定义域名（可选）
# routes = [
#   { pattern = "glowup-beauty.com", custom_domain = true }
# ]
